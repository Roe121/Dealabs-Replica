{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center">Ajouter un Deal</h2>
        <div class="card p-4 shadow">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary rounded-pill mt-3" type="submit">Ajouter</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
